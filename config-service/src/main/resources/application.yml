spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:       
              search-locations: classpath:/shared
  security:
    user:
      name: user
      password: password

server:
  port: 8887

